import { SettingsViewPage } from '@/features/settings/components';

export const metadata = {
  title: 'Dashboard : Settings'
};

export default async function Page() {
  return <SettingsViewPage />;
}
